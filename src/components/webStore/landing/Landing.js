import React from "react";
import { Grid } from "semantic-ui-react";

const Landing = () => (
  <Grid.Row style={{ height: "45vh", backgroundColor: "#13A1CB" }}>
    <Grid rows="1" padded>
      <Grid.Row>
        <Grid.Column />
      </Grid.Row>
    </Grid>
  </Grid.Row>
);

export default Landing;
